# %%
from datetime import datetime


params = {
    "host":'the host',
    "port":'the port',
    "user":'the user',
    "-a":'',
    "-b":'',
    "-c":'',
    "-d":'',
    "database":'the host',
}

now = datetime.now()
now.strftime('%Y%m%d%H%I')